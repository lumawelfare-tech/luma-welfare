{
  "name": "luma-welfare",
  "version": "0.1.0",
  "private": true,
  "description": "Luma Welfare membership and contribution management platform",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "typecheck": "cd frontend && npx tsc --noEmit",
    "lint": "npm run lint -w frontend",
    "test": "npm run test -w frontend",
    "test:qualify": "node --test --import tsx scripts/__tests__/qualify.test.ts",
    "test:rls": "npx vitest run --config frontend/vitest.workspace-rls.ts",
    "scan:bundle": "npx tsx scripts/scan-client-bundle.ts",
    "guard:backend": "npx tsx scripts/guard-no-legacy-backend.ts",
    "check:legal": "npx tsx scripts/check-legal-config.ts",
    "test:all": "npm run test && npm run test:qualify && npm run test:e2e",
    "audit": "npm audit",
    "audit:fix": "npm audit fix",
    "audit:security": "npx tsx scripts/dependency-audit.ts",
    "security:check": "npm audit && npm run audit:security",
    "admin:bootstrap": "npx tsx scripts/admin-bootstrap.ts",
    "deploy:functions": "bash scripts/deploy-edge-functions.sh",
    "verify:deploy": "bash scripts/verify-deploy.sh",
    "verify:phase8": "npx tsx scripts/phase8-smoke-checklist.ts",
    "test:e2e": "npx playwright test",
    "test:e2e:ui": "npx playwright test --ui",
    "lighthouse:smoke": "npx --yes lighthouse@12 http://127.0.0.1:4173 --only-categories=performance,accessibility,best-practices,seo --chrome-flags=--headless --output=json --output-path=./lighthouse-report.json --quiet",
    "prepare": "husky || true"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "unpdf": "0.12.1"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "bash -c 'cd frontend && npx tsc --noEmit --project tsconfig.app.json'"
    ]
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@supabase/supabase-js": "^2.45.0",
    "@types/node": "^24.13.3",
    "@types/pg": "^8.23.1",
    "@vercel/node": "^7.0.0",
    "dotenv": "^16.4.5",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "pg": "^8.23.0",
    "tsx": "^4.19.0",
    "typescript": "~6.0.2"
  }
}
