/**
 * Root re-export for monorepo Root Directory = "." deployments.
 * Canonical implementation lives in frontend/api/cron (for Root Directory = frontend/).
 */
export { default } from '../frontend/api/cron/health-check'
